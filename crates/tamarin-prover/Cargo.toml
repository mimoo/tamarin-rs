[package]
name = "tamarin-prover"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
description = "The Tamarin prover for security protocol verification (Rust port)."

[lib]
name = "tamarin_prover"
path = "src/lib.rs"

[[bin]]
name = "tamarin-rs"
path = "src/main.rs"

[dependencies]
tamarin-utils = { workspace = true }
tamarin-term = { workspace = true }
tamarin-parser = { workspace = true }
tamarin-theory = { workspace = true }
tamarin-sapic = { workspace = true }
tamarin-server = { workspace = true }
tamarin-accountability = { workspace = true }
tokio = { version = "1", features = ["rt-multi-thread", "macros"] }
mimalloc = { version = "0.1", default-features = false }
rayon = { workspace = true }
clap = { workspace = true }
# `--state-audit` report serialisation only — no parity-gated output goes
# through it (see `src/state_audit.rs`).  Already in the graph via
# tamarin-server, so this costs no extra build.
serde_json = { workspace = true }

[dev-dependencies]
tamarin-test-support = { workspace = true }

[lints]
workspace = true
